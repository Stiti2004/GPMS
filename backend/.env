DATABASE_USER=your_user          # PostgreSQL username
DATABASE_PASSWORD=your_password  # PostgreSQL password
DATABASE_HOST=localhost         # PostgreSQL host (e.g., localhost or your database IP)
DATABASE_PORT=5432              # Default PostgreSQL port
DATABASE_NAME=panchayat_db      # Your database name
JWT_SECRET=your_jwt_secret      # Secret for JWT token generation
